alias .. 'cd ..'
alias ... 'cd ../..'
alias - 'cd -'

alias d "dirs -v -l"

alias pc "pushd (pwd)"
alias pu "pushd"
alias po "popd"

alias serve_http 'python3 --version; and python3 -m http.server; or python -m SimpleHTTPServer'
